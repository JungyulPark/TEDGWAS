# Reproduction of the frequency sensitivity revision

The primary reference-frequency results remain unchanged. Actual cohort-frequency MR and colocalization sensitivity were completed on 5 September 2026. See `../provenance/maf/sensitivity_verification_v2.json` and Supplementary Data 4–6. The original 7,219 primary estimates were reproduced; all 81 baseline colocalization rows matched. A reference-frequency identity control and an independent primary-MR formula check passed.

Run `python audit_submission_v2.py` for source-to-manuscript and DOCX checks. `build_documents_v2.py` builds from the repository-root master and existing cover-letter Markdown. `build_figures.py` rebuilds unchanged primary Figures 1–3/S2. It preserves the supplied Figure S1 unless `--tissue-points` is set to an authorized local point-level CSV. The distributed tissue CSV contains group summaries only. The Figure S1 point-source hash in the original verification record identifies its local build input, not the distributed aggregate CSV. Re-render and inspect every Word page after rebuilding. Use the bundled requirements and Pandoc.

## Analysis scripts and local inputs

`analysis_scripts/` preserves the exact executed scripts. Their original analysis-workspace layout was `work/`: scripts at that root, `revision/` holding this repository clone, `maf_sensitivity_20260905/inputs/` holding local extracts, and `data/` holding the official AF gzip. Some preparation paths refer to C:/ProjectTEDGWAS and the earlier authorized local input bundle. To relocate, create a new script version and set those paths explicitly. The current repository-root manuscript is the sole editable manuscript source. The analysis scripts' baseline checks refer to the preserved candidate_20260905 reference results.

Order: prepare_maf_sensitivity_inputs_v1.py; run_maf_baseline_v1.R [analysis directory]; extract_eqtlgen_af_v1.py; run_maf_sensitivity_v1.R [analysis directory] [normalized AF CSV]; run_maf_coloc_v1.py [analysis directory] [normalized AF CSV] [canonical coloc CSV]; export_maf_validation_inputs_v1.R [analysis directory]; verify_maf_results_v2.py. Execute one analysis at a time. Do not overwrite versioned outputs. The AF extractor reads the entire gzip to verify CRC, validates frequencies against genotype counts, matches GRCh37 coordinates, and does not impute absent variants.

The final harmonization audit is the v2 one-to-one allele-record comparison. The R run's rsID-only intermediate QC and the v1 audit are superseded because multiallelic outcome rows can share an rsID; those intermediate counts must not be reported. This correction did not alter any MR estimate. Final changes were 5 added/5 removed records in BBJ, 5/3 in UKB and 6/3 in FinnGen; no unexplained changes or retained outcome sign flips remained.

Raw source extracts, AF rows, genotype counts, harmonized SNP records and individual-level tissue data are excluded from GitHub. `inputs_manifest.json` identifies the earlier five local coloc inputs. `../provenance/maf/local_input_manifest.json` records the GWAS/eQTL/reference input paths and hashes. `../provenance/maf/af_input_verification_v1.json` identifies the new AF source and normalized extract. Download from https://www.eqtlgen.org/cis-eqtls.html. The server certificate was expired at this run; the user explicitly authorized an exception for that single public file. No global TLS setting was changed. The download hash records the received file and is not a publisher authenticity checksum. Future downloads should use valid HTTPS or an independently verified copy; no automatic certificate-bypass downloader is distributed.

Remaining author and journal checks are in REVIEW_REPORT_KO.md. A numerical PASS does not certify causal assumptions, all possible sensitivity analyses, author declarations, or submission acceptance.
